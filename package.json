{
  "name": "shopping-list",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared shopping lists on a table — a companion to the Cook Book.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/generate-icons.mjs",
    "test:rules": "PATH=\"/opt/homebrew/opt/openjdk/bin:/usr/local/opt/openjdk/bin:$PATH\" firebase emulators:exec --only firestore --project rules-test \"vitest run --config vitest.rules.config.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@resvg/resvg-js": "^2.6.2",
    "firebase-tools": "^15.29.0",
    "jsdom": "^29.1.1",
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  }
}
